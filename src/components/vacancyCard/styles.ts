import { createStyles, px } from '@mantine/core';

export const useVacancyCardStyles = createStyles((theme) => ({
  cardWrapper: {},
  cardTitleWrapper: {},
  cardTitle: {},
  cardSalaryWrapper: {},
  cardSalary: {},
  cardSchedule: {},
  location: {},
}));
